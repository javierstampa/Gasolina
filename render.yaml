services:
  - type: web
    name: gasall
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python preload.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300
    healthCheckPath: /api/fuels
    autoDeploy: true
